"""
import modularity2  # this could be anyhing but its a file that I creted which is code from modularity section of a plural sight training sessoin

type(modularity2)
dir(modularity2)  # shows all object associated with the module

dir(modularity2.fetch_words) # shows all objects --including system objects -- associated with the funtion
modularity2.fetch_words.__name__ # shows the name of the funtion
"""